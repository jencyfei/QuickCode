# 📁 QuickCode 项目结构分析

本文档详细说明了 QuickCode 项目的文件结构和各模块功能。

## 🎯 项目概述

QuickCode 是一个专注于「快递取件码识别与管理」的 Android 应用，采用 Kotlin + Jetpack Compose 开发，完全开源，无任何使用限制。

## 📂 目录结构

```
QuickCode/
├── android/                    # Android 原生应用（核心模块）
├── backend/                    # 后端 API 服务（可选）
├── frontend/                   # Web 前端应用（可选）
├── docs/                       # 项目文档
├── scripts/                    # 工具脚本
├── tools/                      # 辅助工具
├── index.html                  # 官网首页
├── README.md                   # 项目说明
├── .gitignore                  # Git 忽略文件
└── PROJECT_STRUCTURE.md        # 本文件
```

---

## 📱 Android 应用（核心模块）

### 目录结构

```
android/
├── app/                        # 应用主模块
│   ├── src/main/
│   │   ├── java/com/sms/tagger/
│   │   │   ├── MainActivity.kt          # 主 Activity，应用入口
│   │   │   ├── SmsAgentApplication.kt    # Application 类，全局初始化
│   │   │   │
│   │   │   ├── data/                    # 数据层
│   │   │   │   ├── model/               # 数据模型
│   │   │   │   │   ├── Models.kt        # 通用数据模型
│   │   │   │   │   └── TagRule.kt       # 标签规则模型
│   │   │   │   ├── remote/              # 网络请求层
│   │   │   │   │   ├── ApiService.kt    # API 接口定义
│   │   │   │   │   ├── AuthInterceptor.kt # 认证拦截器
│   │   │   │   │   └── RetrofitClient.kt # Retrofit 客户端
│   │   │   │   └── repository/          # 数据仓库
│   │   │   │       ├── AuthRepository.kt # 认证仓库
│   │   │   │       ├── SmsRepository.kt  # 短信仓库
│   │   │   │       └── TagRepository.kt  # 标签仓库
│   │   │   │
│   │   │   ├── ui/                      # UI 层
│   │   │   │   ├── screens/             # 页面组件
│   │   │   │   │   ├── ExpressScreen.kt      # 快递管理页面（核心）
│   │   │   │   │   ├── ExpressPickedScreen.kt # 已取快递页面
│   │   │   │   │   ├── SmsListScreen.kt      # 短信列表页面
│   │   │   │   │   ├── SettingsScreen.kt     # 设置页面
│   │   │   │   │   ├── RuleManageScreen.kt   # 规则管理页面
│   │   │   │   │   ├── ClassificationRuleScreen.kt # 分类规则页面
│   │   │   │   │   ├── DebugLogScreen.kt     # 调试日志页面
│   │   │   │   │   └── LogViewerScreen.kt    # 日志查看器页面
│   │   │   │   ├── components/          # 可复用组件
│   │   │   │   │   ├── GlassComponents.kt    # 玻璃拟态组件
│   │   │   │   │   ├── LimitDialogs.kt      # 限制提示对话框
│   │   │   │   │   └── PrivacyPolicyDialog.kt # 隐私政策对话框
│   │   │   │   ├── theme/                # 主题配置
│   │   │   │   │   ├── Color.kt         # 颜色定义
│   │   │   │   │   ├── Theme.kt         # 主题配置
│   │   │   │   │   └── Type.kt          # 字体配置
│   │   │   │   └── viewmodel/            # ViewModel
│   │   │   │       └── ExpressViewModel.kt # 快递 ViewModel
│   │   │   │
│   │   │   └── util/                     # 工具类（核心业务逻辑）
│   │   │       ├── SmsReader.kt           # 短信读取工具
│   │   │       ├── ExpressExtractor.kt    # 快递信息提取（核心）
│   │   │       ├── ExpressCompanyDetector.kt # 快递公司识别
│   │   │       ├── StrictExpressFilter.kt   # 严格快递过滤
│   │   │       ├── ExpressGroupByDate.kt    # 按日期分组
│   │   │       ├── ExpressDataCache.kt      # 快递数据缓存
│   │   │       ├── SmsReceiver.kt          # 短信接收器（BroadcastReceiver）
│   │   │       ├── SmsReceiverDiagnostics.kt # 短信接收诊断
│   │   │       ├── RuleEngine.kt          # 规则引擎（短信分类）
│   │   │       ├── NotificationHelper.kt  # 通知助手
│   │   │       ├── SmsListCache.kt        # 短信列表缓存
│   │   │       ├── SmsListSettings.kt     # 短信列表设置
│   │   │       ├── TimeWindowSettings.kt  # 时间窗口设置
│   │   │       ├── PreferencesManager.kt   # 偏好设置管理
│   │   │       ├── PrivacyPolicyManager.kt # 隐私政策管理
│   │   │       ├── DeviceIdManager.kt     # 设备 ID 管理
│   │   │       ├── ActivationManager.kt   # 激活管理（开源版已简化）
│   │   │       ├── UsageLimitManager.kt  # 使用限制管理（开源版已简化）
│   │   │       ├── TrialManager.kt       # 试用管理（开源版已简化）
│   │   │       ├── FernetDecryptor.kt    # Fernet 解密工具（已废弃）
│   │   │       ├── AppLogger.kt          # 应用日志
│   │   │       ├── InMemoryLogger.kt     # 内存日志
│   │   │       ├── LogFileWriter.kt      # 日志文件写入
│   │   │       ├── LogControlSettings.kt  # 日志控制设置
│   │   │       └── LogInterceptor.kt     # 日志拦截器
│   │   │
│   │   ├── res/                           # 资源文件
│   │   │   ├── mipmap-*/                  # 应用图标（多密度）
│   │   │   ├── drawable/                  # 可绘制资源
│   │   │   │   └── yunda.png             # 韵达快递图标
│   │   │   ├── values/                    # 值资源
│   │   │   │   ├── strings.xml            # 字符串资源
│   │   │   │   ├── colors.xml             # 颜色资源
│   │   │   │   └── themes.xml             # 主题资源
│   │   │   └── xml/                       # XML 配置
│   │   │       ├── backup_rules.xml      # 备份规则
│   │   │       └── data_extraction_rules.xml # 数据提取规则
│   │   │
│   │   └── AndroidManifest.xml            # Android 清单文件
│   │
│   ├── build.gradle                       # 应用构建配置
│   └── proguard-rules.pro                 # ProGuard 规则
│
├── build.gradle                           # 项目构建配置
├── settings.gradle                        # 项目设置
├── gradle.properties                     # Gradle 属性
├── local.properties                       # 本地属性（不提交）
├── gradlew.bat                            # Gradle 包装器（Windows）
├── gradlew                                # Gradle 包装器（Linux/Mac）
├── gradle/                                # Gradle 包装器文件
│   └── wrapper/
│       ├── gradle-wrapper.jar
│       └── gradle-wrapper.properties
│
├── build_apk.bat                          # 构建 APK 脚本
├── convert_icon.py                        # 图标转换脚本
└── sms-release-key.jks                    # 签名密钥（不提交）
```

### 核心模块说明

#### 1. 快递提取模块 (`util/ExpressExtractor.kt`)

**功能**: 从短信内容中提取快递信息

**主要方法**:
- `extractExpressInfo()` - 提取快递信息
- `detectExpressCompany()` - 识别快递公司
- `extractPickupCode()` - 提取取件码
- `extractAddress()` - 提取地址

**支持的快递平台**:
- 菜鸟驿站
- 兔喜生活
- 京东物流
- 顺丰速运
- 中通快递
- 圆通速递
- 韵达速递

#### 2. 短信读取模块 (`util/SmsReader.kt`)

**功能**: 读取系统短信数据库

**主要方法**:
- `readAllSms()` - 读取所有短信
- `readSmsByDateRange()` - 按日期范围读取
- `getSmsCount()` - 获取短信数量

#### 3. 规则引擎 (`util/RuleEngine.kt`)

**功能**: 根据规则对短信进行分类

**主要方法**:
- `classifySms()` - 分类短信
- `matchRule()` - 匹配规则
- `evaluateCondition()` - 评估条件

#### 4. UI 组件

**主要页面**:
- `ExpressScreen.kt` - 快递管理主页面，显示未取快递
- `ExpressPickedScreen.kt` - 已取快递页面
- `SmsListScreen.kt` - 短信列表页面
- `SettingsScreen.kt` - 设置页面

**主要组件**:
- `GlassComponents.kt` - 玻璃拟态效果组件
- `LimitDialogs.kt` - 限制提示对话框

---

## 🐍 后端服务（可选）

### 目录结构

```
backend/
├── app/                        # 应用主目录
│   ├── main.py                 # FastAPI 应用入口
│   ├── config.py               # 配置文件
│   ├── database.py             # 数据库配置
│   │
│   ├── routers/                # API 路由
│   │   ├── auth.py             # 认证相关路由
│   │   ├── sms.py              # 短信管理路由
│   │   ├── tags.py             # 标签管理路由
│   │   └── extraction_rules.py # 规则管理路由
│   │
│   ├── models/                 # 数据模型（SQLAlchemy）
│   │   ├── user.py             # 用户模型
│   │   ├── sms.py              # 短信模型
│   │   ├── tag.py              # 标签模型
│   │   └── extraction_rule.py # 规则模型
│   │
│   ├── schemas/                # Pydantic 模式
│   │   ├── auth.py             # 认证模式
│   │   ├── sms.py              # 短信模式
│   │   ├── tag.py              # 标签模式
│   │   └── extraction_rule.py # 规则模式
│   │
│   ├── services/               # 业务逻辑层
│   │   ├── sms_classifier.py   # 短信分类器
│   │   └── rule_engine.py      # 规则引擎
│   │
│   ├── dependencies/           # 依赖注入
│   │   └── auth.py             # 认证依赖
│   │
│   ├── utils/                  # 工具函数
│   │   └── security.py         # 安全工具（JWT、密码哈希等）
│   │
│   └── init_rule_templates.py  # 初始化规则模板
│
├── requirements.txt            # Python 依赖
└── venv/                       # 虚拟环境（不提交）
```

### 主要功能

- **用户认证**: JWT 令牌认证
- **短信管理**: CRUD 操作
- **规则管理**: 自定义分类规则
- **标签管理**: 短信标签系统

---

## 🌐 Web 前端（可选）

### 目录结构

```
frontend/
├── src/
│   ├── views/                  # 页面组件
│   │   ├── Login.vue           # 登录页面
│   │   ├── SmsList.vue        # 短信列表
│   │   ├── SmsListNew.vue     # 短信列表（新版本）
│   │   ├── TagManage.vue     # 标签管理
│   │   ├── TagManageNew.vue   # 标签管理（新版本）
│   │   ├── RuleConfig.vue     # 规则配置
│   │   ├── ExpressDetail.vue # 快递详情
│   │   ├── SmsImport.vue      # 短信导入
│   │   └── Settings.vue       # 设置页面
│   │
│   ├── components/            # 公共组件
│   │   ├── HelloWorld.vue
│   │   └── RuleList.vue
│   │
│   ├── api/                    # API 封装
│   │   ├── request.js         # Axios 配置
│   │   ├── auth.js            # 认证 API
│   │   ├── sms.js             # 短信 API
│   │   ├── tag.js             # 标签 API
│   │   └── extractionRules.js # 规则 API
│   │
│   ├── store/                  # 状态管理（Pinia）
│   │   ├── index.js           # Store 入口
│   │   └── user.js            # 用户状态
│   │
│   ├── router/                 # 路由配置
│   │   └── index.js
│   │
│   ├── utils/                  # 工具函数
│   │   ├── smsParser.js       # 短信解析
│   │   ├── smsImportParser.js # 短信导入解析
│   │   ├── addressExtractor.test.js # 地址提取测试
│   │   └── environment.js    # 环境配置
│   │
│   ├── assets/                 # 静态资源
│   │   ├── icons/             # 图标
│   │   │   ├── cainiao.png    # 菜鸟图标
│   │   │   ├── jd.png         # 京东图标
│   │   │   ├── sf.png         # 顺丰图标
│   │   │   └── ...
│   │   ├── styles/            # 样式文件
│   │   │   └── global.css
│   │   └── glassmorphism.css # 玻璃拟态样式
│   │
│   ├── App.vue                # 根组件
│   └── main.js                # 入口文件
│
├── public/                     # 公共资源
├── dist/                       # 构建输出（不提交）
├── node_modules/              # 依赖包（不提交）
├── package.json               # 项目配置
├── vite.config.js             # Vite 配置
└── capacitor.config.json       # Capacitor 配置
```

---

## 📚 文档目录

```
docs/
├── 1.新增通知.md              # 新增通知功能说明
├── 2.无法开启通知.md          # 通知问题排查
├── 3.排查.md                  # 问题排查指南
├── ACTIVATION_TODO.md         # 激活功能待办
├── ACTIVATION_CODE_DIFFERENCE_ANALYSIS.md # 激活码差异分析
├── GRADLE_BUILD_MIGRATION_GUIDE.md # Gradle 构建迁移指南
├── TRIAL_EXPIRATION_FIX.md   # 试用期到期修复
├── GITHUB_RELEASE_UPLOAD.md  # GitHub Release 上传指南
└── ...                        # 其他文档
```

---

## 🛠️ 工具脚本

```
scripts/
├── init_db.py                 # 数据库初始化
├── test_auth.py               # 认证测试
├── test_db_connection.py      # 数据库连接测试
├── debug_jwt.py              # JWT 调试
├── diagnose_adb_connection.ps1 # ADB 连接诊断
├── view_sms_logcat.ps1       # 查看 SMS Logcat
├── cleanup_gradle_cache.ps1   # 清理 Gradle 缓存
├── upload_apk_to_github.ps1  # 上传 APK 到 GitHub
├── QUICK_LOGCAT_GUIDE.md     # Logcat 快速指南
└── QUICK_UPLOAD.md           # 快速上传指南
```

---

## 🔑 关键文件说明

### Android 应用

| 文件 | 说明 |
|------|------|
| `MainActivity.kt` | 应用主入口，处理导航和权限 |
| `ExpressScreen.kt` | 快递管理核心页面 |
| `ExpressExtractor.kt` | 快递信息提取核心逻辑 |
| `SmsReader.kt` | 短信读取工具 |
| `RuleEngine.kt` | 短信分类规则引擎 |
| `AndroidManifest.xml` | Android 清单文件，定义权限和组件 |

### 配置文件

| 文件 | 说明 |
|------|------|
| `build.gradle` | Gradle 构建配置 |
| `.gitignore` | Git 忽略文件配置 |
| `README.md` | 项目说明文档 |
| `PROJECT_STRUCTURE.md` | 项目结构文档（本文件） |

---

## 📊 数据流

### 快递提取流程

```
短信接收 (SmsReceiver)
    ↓
短信读取 (SmsReader)
    ↓
快递提取 (ExpressExtractor)
    ↓
快递公司识别 (ExpressCompanyDetector)
    ↓
严格过滤 (StrictExpressFilter)
    ↓
按日期分组 (ExpressGroupByDate)
    ↓
数据缓存 (ExpressDataCache)
    ↓
UI 显示 (ExpressScreen)
```

### 短信分类流程

```
短信读取 (SmsReader)
    ↓
规则匹配 (RuleEngine)
    ↓
分类结果
    ↓
标签应用
    ↓
UI 显示 (SmsListScreen)
```

---

## 🔒 安全说明

### 已移除的敏感功能

- ✅ 激活码系统已移除
- ✅ 试用期限制已移除
- ✅ 使用次数限制已移除
- ✅ 所有功能完全开放

### 不提交的文件

以下文件不应提交到 Git：

- `*.jks` / `*.keystore` - 签名密钥
- `local.properties` - 本地配置
- `*.apk` - 构建产物
- `venv/` / `node_modules/` - 依赖目录
- `build/` - 构建目录
- `.env` - 环境变量

---

## 📝 开发建议

### 代码组织

1. **按功能模块组织**: 每个功能模块应该有清晰的职责
2. **工具类独立**: 可复用的工具类放在 `util/` 目录
3. **UI 组件复用**: 公共 UI 组件放在 `ui/components/` 目录
4. **数据层分离**: 数据模型、仓库、API 分离

### 命名规范

- **Kotlin**: 使用驼峰命名，类名首字母大写
- **资源文件**: 使用小写字母和下划线
- **包名**: 使用反向域名格式

### 提交规范

使用清晰的提交信息，参考 [贡献指南](README.md#-贡献指南)

---

## 🔗 相关链接

- [README.md](README.md) - 项目主文档
- [GitHub 仓库](https://github.com/jencyfei/QuickCode) - 项目地址
- [问题反馈](https://github.com/jencyfei/QuickCode/issues) - 提交问题

---

**最后更新**: 2026-01-05

