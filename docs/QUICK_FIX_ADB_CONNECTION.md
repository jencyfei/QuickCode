# ADB设备连接问题快速解决指南

## 当前状态

✅ **Full版应用已安装**在手机上  
✅ **APK文件存在**（路径：`android/app/build/outputs/apk/full/release/sms-agent-fullRelease-1.3.0.apk`）  
❌ **`adb devices` 返回空**（设备未连接）

---

## 快速解决步骤

### 第一步：检查USB连接

1. **确认USB线已连接**
   - 手机通过USB线连接到电脑
   - 尝试更换USB端口
   - 尝试更换USB线（某些线只支持充电）

2. **检查USB连接模式**
   - 手机上可能弹出"USB连接方式"通知
   - 选择"文件传输"或"MTP模式"
   - 不要选择"仅充电"

---

### 第二步：启用USB调试

1. **打开开发者选项**
   - 设置 → 关于手机
   - 连续点击"版本号"7次
   - 提示"您已处于开发者模式"

2. **启用USB调试**
   - 返回设置 → 开发者选项
   - 启用"USB调试"
   - 启用"仅充电模式下允许ADB调试"（小米手机）

---

### 第三步：授权电脑

1. **首次连接时**
   - 手机上弹出"允许USB调试吗？"对话框
   - **勾选"始终允许这台计算机"**
   - 点击"确定"

2. **如果已经授权但连接失败**
   - 开发者选项 → "撤销USB调试授权"
   - 重新连接USB
   - 重新授权

---

### 第四步：重启ADB服务

在PowerShell中运行：

```powershell
adb kill-server
adb start-server
adb devices
```

---

### 第五步：验证连接

运行以下命令：

```powershell
adb devices
```

**预期输出：**
```
List of devices attached
XXXXXXXX    device
```

如果显示 `unauthorized`：
- 在手机上点击"允许USB调试"

如果仍然是空的：
- 继续下面的排查步骤

---

## 品牌特定设置

### 小米手机

- ✅ 启用"仅充电模式下允许ADB调试"
- ✅ 关闭"MIUI优化"（可选）
- ✅ 安装小米USB驱动（如果设备管理器显示感叹号）

### 华为/荣耀手机

- ✅ 启用"USB调试（安全设置）"
- ✅ 关闭"纯净模式"
- ✅ 安装华为手机助手（如果需要驱动）

### OPPO/OnePlus手机

- ✅ 启用"USB调试（安全设置）"
- ✅ 关闭"纯净模式"

### VIVO手机

- ✅ 启用"USB调试（安全设置）"
- ✅ 关闭"纯净模式"

---

## 检查Windows设备管理器

1. **打开设备管理器**
   - 按 `Win + X`
   - 选择"设备管理器"

2. **查找Android设备**
   - 查看是否有"Android Phone"或"便携设备"
   - 如果有黄色感叹号，说明驱动有问题

3. **安装驱动**
   - 根据手机品牌安装对应驱动
   - 或使用手机厂商的助手软件安装驱动

---

## 替代方案：WiFi连接

如果USB连接一直有问题，可以使用WiFi连接：

### 前提条件
- USB连接曾经成功过一次（用于初始设置）

### 步骤

```powershell
# 1. 先用USB连接一次（如果可能）
adb devices

# 2. 启用WiFi调试
adb tcpip 5555

# 3. 在手机上查看WiFi IP地址
#    设置 → WLAN → 点击已连接的WiFi → 查看IP地址
#    假设IP是 192.168.1.100

# 4. 通过WiFi连接
adb connect 192.168.1.100:5555

# 5. 断开USB，验证连接
adb devices
```

---

## 连接成功后，查看日志

### 方法1：查看所有日志

```powershell
adb logcat -c
adb logcat
```

### 方法2：过滤特定关键词

```powershell
adb logcat -c
adb logcat | Select-String -Pattern "SmsReader|10684|9-5-5038|菜鸟驿站"
```

### 方法3：保存日志到文件

```powershell
adb logcat -c
# 然后在手机上操作应用...

# 按 Ctrl+C 停止，然后保存
$timestamp = Get-Date -Format "yyyyMMdd_HHmmss"
adb logcat -d > "logcat_$timestamp.txt"
```

---

## 重新安装应用（如果需要）

### 使用正确的APK路径

```powershell
# Full版
adb install -r android/app/build/outputs/apk/full/release/sms-agent-fullRelease-1.3.0.apk

# Trial版
adb install -r android/app/build/outputs/apk/trial/release/sms-agent-trialRelease-1.3.0-trial.apk

# Debug版
adb install -r android/app/build/outputs/apk/debug/sms-agent-debug-1.3.0.apk
```

---

## 快速诊断命令

运行以下命令进行快速诊断：

```powershell
# 1. 检查ADB版本
adb version

# 2. 重启ADB服务
adb kill-server
adb start-server

# 3. 检查设备
adb devices

# 4. 查看设备详细信息（如果连接成功）
adb devices -l

# 5. 检查应用是否已安装（如果连接成功）
adb shell pm list packages | Select-String "sms.tagger"
```

---

## 详细排查指南

如果以上步骤都无法解决，请查看详细排查指南：

📄 **docs/ADB_DEVICES_EMPTY_TROUBLESHOOTING.md**

包含：
- 完整的故障排查步骤
- 所有品牌手机的特定设置
- 驱动安装指南
- 常见问题解答

---

## 总结

**关键点：**
1. ✅ USB线连接且支持数据传输
2. ✅ 启用USB调试
3. ✅ 授权电脑
4. ✅ 选择正确的USB连接模式
5. ✅ 品牌特定设置已配置
6. ✅ 驱动已安装（如果设备管理器显示感叹号）

**APK文件路径：**
- Full版：`android/app/build/outputs/apk/full/release/sms-agent-fullRelease-1.3.0.apk`
- Trial版：`android/app/build/outputs/apk/trial/release/sms-agent-trialRelease-1.3.0-trial.apk`

连接成功后，您可以查看Logcat日志，排查短信识别问题！

