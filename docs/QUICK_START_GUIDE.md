# 🚀 快速启动指南

## 服务状态检查

### 检查服务是否运行

**Windows PowerShell**:
```powershell
# 检查前端 (3000端口)
netstat -ano | findstr ":3000"

# 检查后端 (10043端口)
netstat -ano | findstr ":10043"
```

如果看到 `LISTENING`，说明服务正在运行 ✅

---

## 启动服务

### 方式一：使用启动脚本（推荐）

**启动后端**：
```bash
双击运行: scripts/start_backend_10043.bat
```

**启动前端**：
```bash
双击运行: scripts/start_frontend.bat
```

### 方式二：命令行启动

**启动后端**：
```bash
cd backend
python -m uvicorn app.main:app --reload --port 10043
```

**启动前端**：
```bash
cd frontend
npm run dev
```

---

## 访问地址

| 服务 | 地址 | 说明 |
|------|------|------|
| 前端界面 | http://localhost:3000 | 主页（自动跳转到快递详情） |
| 短信列表 | http://localhost:3000/sms-list | 短信列表页面 |
| 快递详情 | http://localhost:3000/express-detail | 快递详情页面 |
| 短信导入 | http://localhost:3000/sms-import | 导入短信页面 |
| 标签管理 | http://localhost:3000/tag-manage | 标签管理页面 |
| 设置 | http://localhost:3000/settings | 设置页面 |
| 后端API | http://localhost:10043/docs | API文档 (Swagger) |

---

## 常见问题

### ❌ 问题1: 无法访问 http://localhost:3000/sms-list

**原因**：前端服务未启动

**解决方案**：
1. 运行 `scripts/start_frontend.bat` 启动前端
2. 或手动进入 `frontend` 目录执行 `npm run dev`
3. 等待看到 "Local: http://localhost:3000" 提示

### ❌ 问题2: 页面显示但无数据/API错误

**原因**：后端服务未启动或端口不匹配

**解决方案**：
1. 确认后端运行在 10043 端口：`netstat -ano | findstr ":10043"`
2. 运行 `scripts/start_backend_10043.bat` 启动后端
3. 检查 `frontend/vite.config.js` 中的代理配置是否指向 `http://localhost:10043`

### ❌ 问题3: 端口被占用

**检查占用进程**：
```powershell
netstat -ano | findstr ":3000"
# 或
netstat -ano | findstr ":10043"
```

**停止占用进程**：
```powershell
taskkill /F /PID <进程ID>
```

### ❌ 问题4: npm run dev 失败

**原因**：依赖未安装

**解决方案**：
```bash
cd frontend
npm install
npm run dev
```

---

## 开发环境配置

### 端口配置

| 配置项 | 文件 | 说明 |
|--------|------|------|
| 前端端口 | `frontend/vite.config.js` | 默认 3000 |
| 后端端口 | `scripts/start_backend_10043.bat` | 默认 10043 |
| API代理 | `frontend/vite.config.js` | 指向后端端口 |

### 修改端口

**修改前端端口**:
编辑 `frontend/vite.config.js`:
```javascript
server: {
  port: 3000,  // 改为你想要的端口
  host: '0.0.0.0',
  // ...
}
```

**修改后端端口**:
编辑 `scripts/start_backend_10043.bat`:
```batch
uvicorn app.main:app --reload --host 0.0.0.0 --port 10043
                                                      ^^^^^ 改为你想要的端口
```

**同时记得更新前端代理配置**：
```javascript
proxy: {
  '/api': {
    target: 'http://localhost:10043',  // 改为新的后端端口
    changeOrigin: true,
  }
}
```

---

## 当前配置状态 ✅

- ✅ 前端运行在: **http://localhost:3000**
- ✅ 后端运行在: **http://localhost:10043**
- ✅ API代理配置: **已匹配**
- ✅ 自动标签识别: **已启用**
- ✅ 取件码提取: **支持横杠格式**

---

## 测试自动标签功能

### 1. 访问短信导入页面
```
http://localhost:3000/sms-import
```

### 2. 粘贴测试短信
```
【菜鸟驿站】您的包裹已到站，凭7-5-3028到郑州市北文雅小区6号楼102店取件。
```

### 3. 点击"开始导入" → "确认导入"

### 4. 查看结果
- 自动打上 📦 快递 标签
- 提取取件码: 7-5-3028
- 在快递标签列表中可见

---

## 完整启动流程

1. **启动后端** (10043端口)
   ```
   双击: scripts/start_backend_10043.bat
   等待看到: "Application startup complete"
   ```

2. **启动前端** (3000端口)
   ```
   双击: scripts/start_frontend.bat
   等待看到: "Local: http://localhost:3000"
   ```

3. **访问应用**
   ```
   浏览器打开: http://localhost:3000
   ```

4. **开始使用**
   - 导入短信测试自动标签识别
   - 查看快递详情页面
   - 管理标签

---

## 技术栈

| 层级 | 技术 | 版本 |
|------|------|------|
| 前端 | Vue 3 + Vite | 3.5.22 + 7.1.7 |
| UI库 | Vant | 4.9.21 |
| 路由 | Vue Router | 4.6.3 |
| 状态 | Pinia | 3.0.3 |
| 后端 | FastAPI + Python | 3.10+ |
| 数据库 | PostgreSQL | - |

---

**更新日期**: 2025-11-07  
**维护者**: SMS Agent Team

