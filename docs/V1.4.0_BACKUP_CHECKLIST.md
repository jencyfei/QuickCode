# v1.4.0 代码备份清单

## 📦 版本信息
- **版本号**: v1.4.0
- **Version Code**: 5
- **Version Name**: 1.4.0
- **打包日期**: 2025年12月1日

## ✅ 打包完成的APK

### Full版
- **文件名**: `sms-agent-fullRelease-1.4.0.apk`
- **路径**: `android/app/build/outputs/apk/full/release/sms-agent-fullRelease-1.4.0.apk`
- **大小**: 10.66 MB
- **修改时间**: 2025-12-01 10:22:47

### Trial版
- **文件名**: `sms-agent-trialRelease-1.4.0-trial.apk`
- **路径**: `android/app/build/outputs/apk/trial/release/sms-agent-trialRelease-1.4.0-trial.apk`
- **大小**: 10.66 MB
- **修改时间**: 2025-12-01 10:25:13

## 📝 版本号配置

### build.gradle配置
```gradle
versionCode 5
versionName "1.4.0"
```

### 设置页面显示
- 位置: `SettingsScreen.kt` - `AppInfoCard()`
- 显示: `v${BuildConfig.VERSION_NAME}` → **v1.4.0**

### 安装时显示
- 从 `AndroidManifest.xml` 自动读取
- 显示为: **1.4.0**

## 🔄 代码保存建议

### 方案1：Git提交（推荐）

```bash
# 进入项目根目录
cd D:\tools\python\mypro\sms_agent

# 添加所有新文件
git add .

# 提交代码
git commit -m "feat: v1.4.0 - 默认短信应用支持

- 实现成为系统默认短信应用的最小能力
- 添加SMS_DELIVER BroadcastReceiver支持
- 创建SmsSendService和SmsWriteHelper (Stub)
- 添加默认短信应用引导页面
- 在设置页面集成默认短信应用入口
- 支持小米14等设备读取所有短信（包括10684开头）

版本号: 1.4.0 (Version Code: 5)
"

# 创建标签
git tag -a v1.4.0 -m "Release v1.4.0 - 默认短信应用支持"

# 查看提交
git log --oneline -1
```

### 方案2：创建分支备份

```bash
# 创建备份分支
git checkout -b backup/v1.4.0-default-sms-support

# 提交代码（同上）
git add .
git commit -m "feat: v1.4.0 - 默认短信应用支持"

# 返回主分支（如果需要）
git checkout main
```

### 方案3：导出代码包

如果需要导出完整代码包作为备份：

```bash
# 创建备份目录
mkdir -p backup/v1.4.0

# 导出代码（排除build目录）
git archive --format=zip --output=backup/v1.4.0/v1.4.0-source.zip HEAD
```

## 📋 主要变更文件清单

### 新建文件
1. `android/app/src/main/java/com/sms/tagger/util/SmsSendService.kt`
2. `android/app/src/main/java/com/sms/tagger/util/SmsWriteHelper.kt`
3. `android/app/src/main/java/com/sms/tagger/util/SmsDefaultAppChecker.kt`
4. `android/app/src/main/java/com/sms/tagger/ui/screens/DefaultSmsGuideScreen.kt`

### 修改文件
1. `android/app/build.gradle` - 版本号更新为1.4.0
2. `android/app/src/main/AndroidManifest.xml` - 权限和配置
3. `android/app/src/main/java/com/sms/tagger/util/SmsReceiver.kt` - SMS_DELIVER支持
4. `android/app/src/main/java/com/sms/tagger/ui/screens/SettingsScreen.kt` - 集成入口

### 新增文档
1. `docs/DEFAULT_SMS_IMPLEMENTATION_TODO.md`
2. `docs/DEFAULT_SMS_IMPLEMENTATION_SUMMARY.md`
3. `docs/V1.4.0_RELEASE_NOTES.md`
4. `docs/V1.4.0_BACKUP_CHECKLIST.md`

## ⚠️ 回退说明

如果后续需要回退到v1.4.0：

### 使用Git标签回退
```bash
git checkout v1.4.0
```

### 使用提交哈希回退
```bash
# 查看v1.4.0的提交哈希
git log --oneline | grep "v1.4.0"

# 回退到该提交
git checkout <commit-hash>
```

## 🔍 验证步骤

安装APK后验证：

1. **版本号显示** ✅
   - 打开应用 → 设置页面
   - 查看"📨 QuickCode"卡片
   - 应显示: **v1.4.0**

2. **默认短信应用功能** ✅
   - 设置页面应显示"默认短信应用"卡片
   - 点击可进入引导页面
   - 可以设置为默认短信应用

3. **功能验证** ✅
   - 设置为默认后，应能读取所有短信
   - 特别是10684开头的短信应能被读取

---

**备份完成时间**: 2025-12-01  
**建议**: 使用Git提交 + 标签的方式保存代码，便于后续管理和回退

