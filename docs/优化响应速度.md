[1 tool called]

继续按“缓存/时间窗口/日志裁剪/懒加载”之后的阶段推进，我建议按以下顺序实施，确保每一步都易于测试和回退：

### 阶段 3：缓存细化 + 增量更新（已完成）  
1. **Express 缓存有效性提升**  
   - 记录最近一次解析所用短信的 `lastMessageId`（或时间戳），下次进入时先看数据库最新短信是否超出范围，若无则完全复用缓存，若有则只解析新增部分并合并进缓存。  
2. **短信列表缓存增量**  
   - 与 Express 同步思路，只对新增短信做排序/插入，替换当前的整列表刷新。  
3. **缓存失效规则**  
   - 把现有 `60s TTL` 替换为 “新短信触发 + 最长 TTL” 组合，避免频繁热更新时缓存没意义。

### 阶段 4：时间窗口配置化（已完成）  
1. **Express 最近 N 天**  
   - 将 `EXPRESS_RECENT_DAYS` 提升为可配置（比如设置页的隐藏开关或远程配置），方便后续调优。  
2. **短信列表最近 N 天**  
   - 同上，加一个“加载更多历史”按钮，只在用户主动操作时扩大范围，默认保持 90 天。  
3. **UI 提示**  
   - 在列表顶部说明“仅展示最近 90 天，可手动加载更早短信”，减少用户疑惑。

### 阶段 5：诊断日志完全开关化（已完成）  
1. **AppLogger 封装**  
   - 提供 `AppLogger.debug { .. }` 形式，内部根据 BuildConfig 或特定开关输出，避免散落判断。  
2. **Express/SmsList 调试 UI**  
   - 可选：在调试菜单里增一个“导出诊断日志”入口，需要时开启，不需要时保持安静。

### 阶段 6：懒加载进一步优化（未开始）  
1. **短信列表分页请求**  
   - 当滑动到尾部时，真正再去数据库加载下一批（而不是一次性取 5000），进一步降低首次耗时。  
2. **快递列表懒加载**  
   - 当快递数量巨大时，按日期分段加载，例如“本月/本周/更早”折叠，展开才渲染。

测试：已运行 `.\gradlew.bat test`，当前全部通过。
后续建议：开始 Stage6 的懒加载细化（短信分页、快递分段渲染），并在真机上观察配置化窗口与诊断日志开关后的体验差异。
